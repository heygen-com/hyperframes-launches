# Asset Descriptions

One line per file. Read this instead of opening every image individually.

To find a specific brand or icon, **grep this file for the brand name in the description text** (e.g. `grep -i 'autodesk' asset-descriptions.md`). The Gemini Vision captions identify what's actually in each file — that's the agent's selector.

The `logo-<hash>.svg` filename prefix is a cheap structural hint (DOM said this SVG was inside a `<header>`, home-link `<a>`, or had an aria-label matching the page brand). It is NOT a content claim — many `logo-*` files are nav icons or decorative shapes. Trust the captions, not the filename prefix.

- favicon.svg — 1KB, The icon consists of two rounded, triangle-like shapes pointing toward each other, featuring a vibrant gradient transition from teal to lime green.
- logo-d2d0b25a.svg — 7KB, This is a wordmark that reads "HeyGen HyperFrames" in black text, positioned to the left of a teal-to-green gradient icon resembling two tilted, rounded rectangles.
- og-image.png — 225KB, The website features a dark-themed interface with neon green text, displaying code snippets for an HTML-based video rendering framework alongside a live preview of the generated animation.
- svgs/logo-7eba73d5.svg — This is a black, thick-lined plus sign icon centered on a white background.
- svgs/svg-b48c83c9.svg — This is a solid black circle containing a white silhouette of the GitHub "Octocat" icon.
- fonts/ABCSolarDisplay-Bold.woff2 — font file
- fonts/TT_Norms_Pro_Bold.woff2 — font file
- fonts/TT_Norms_Pro_DemiBold.woff2 — font file
- fonts/TT_Norms_Pro_Medium.woff2 — font file
- fonts/TT_Norms_Pro_Mono_Regular.woff2 — font file
- fonts/TT_Norms_Pro_Normal.woff2 — font file
